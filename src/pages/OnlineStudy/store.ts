import delay from '@/utils/delay';
import { isArray, toNumber } from 'lodash';
const PAGE_LEN = 10; // 每页个数

interface IState {


}

const defaultState: IState = {


};

export default {
  namespace: 'onlinStudy',
  state: defaultState,
  reducers: {
    updateAllCtypeList: (state: IState, { payload }) => {
      
    },

  },
  effects: {
   
  },
};
